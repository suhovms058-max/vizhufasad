# Отчёт по остаточному юридико-техническому этапу

Дата локальной проверки: 31.08.2026. Ветка: `codex/legal-photo-anonymization`.
Production не изменялся. Платежи и платные запросы GenAPI не выполнялись.

## Подготовлено

### Согласие до первого сбора email

- На `/auth/login` добавлена отдельная пустая отметка согласия на ПД со ссылками на действующие документы.
- Без `accepted=true`, актуальной версии и SHA-256 сервер не нормализует и не сохраняет email и не отправляет код.
- Доказательство акцепта записывается в одной транзакции с challenge: версия, хеш документа, время, действие, хеш IP и ограниченный user-agent.
- После успешной проверки кода запись связывается с `user_id`.
- На `/auth/verify` сохранены только отдельные пустые отметки пользовательского соглашения и 18+; повторное согласие на те же ПД не требуется.

Миграция: `server/drizzle/0022_preauth_personal_data_consent.sql`.

### Локальное обезличивание фото

- Реализован локальный сетево-независимый Python/OpenCV/ONNX Runtime-процесс с обязательными детекторами лиц, текста и автомобильных номеров.
- Нормализованный исходник сохраняется приватно; отдельно создаются очищенные `working.jpg` и thumbnail.
- В оценку фото, GenAPI и модель генерации передаётся только очищенная рабочая копия.
- EXIF, XMP и IPTC отсутствуют в принятом выходном JPEG.
- При выключенной функции, отсутствии модели, ошибке процесса, таймауте, неполном отчёте или некорректном выходе обработка завершается fail closed: карантин и промежуточные файлы очищаются, оценка и генерация не запускаются, ВФ-коин не резервируется и не списывается.
- Документоподобный снимок или изображение с высокой плотностью текста отклоняется целиком тем же fail-closed путём. Частично замаскированный документ не может стать рабочей копией и не передаётся GenAPI.
- Для номеров объединены OpenCV Zoo LPD-YuNet и глобальный YOLOv9 plate detector. Российский Haar-каскад удалён после выявленного ложного срабатывания на чистом фасаде.

Техническая спецификация и ограничения: `tasks/legal-photo-anonymization-spec.md`.

### Юридические тексты и интерфейс

- Политика ПД и согласие на фото получили редакцию `2026-08-31`, согласие на ПД — `2026-08-30`; остальные документы сохранили редакцию `2026-08-28`.
- В политике добавлена структура «цель и субъекты → данные и основание → операции и обработчик → срок и удаление».
- Обновлены предупреждение перед загрузкой и FAQ о хранении фото.
- Формулировки прямо сообщают об ограничениях детекторов и не обещают 100% распознавание.

### Маркетинг и публикация пользовательских работ

- Галерея использует только статический каталог демонстрационных ресурсов.
- Маршрутов и схем автоматической публикации пользовательских фото/результатов или маркетинговых рассылок не найдено.
- Поэтому новые согласия на маркетинг и публикацию не создавались.

## Доказательства проверки

- Целевой набор без поднятой БД: 29 сценариев, 27 пройдено, 2 инфраструктурных сценария корректно пропущены, ошибок 0.
- На заново созданном локальном Docker-контуре после миграций и seed пройдено 17/17 интеграционных сценариев.
- В устойчивых явных пакетах проверены 194 уникальных серверных сценария: функциональных ошибок нет.
- Новые Playwright E2E для согласия до POST email, аналитики fail closed и двух пустых согласий перед загрузкой фото: 12/12 на 360, 390, 768 и 1440 px.
- `npm run check`: TypeScript и production-сборка Next.js, 21 страница — успешно.
- `npm --prefix server run check` — успешно.
- `git diff --check` — успешно; только информационные предупреждения Git о будущей нормализации LF/CRLF.
- Локальная визуальная проверка `/auth/login` на 1440×1000 и 390×844: переполнения по горизонтали нет; после выбора cookie-режима checkbox согласия, обе ссылки и кнопка полностью доступны.
- Реальная открытая выборка российских автомобильных номеров: 100 детерминированно выбранных фотографий, 103 размеченных номера; обработано 100/100, ошибок процесса 0, маскировано 103/103, recall 1,000 при критерии 0,980. p50 2,629 с, p95 4,410 с, максимум 5,542 с.
- На отдельном чистом фасаде 4000×3000 финальная конфигурация дала 0 лиц, 0 текстовых областей, 0 номеров и 0 ложных масок; время 5,822 с.
- Реальная документная выборка FUNSD: 50/50 документов обработаны без ошибок и 50/50 распознаны как документоподобные, document-block recall 1,000. Прямое покрытие всех 8 707 размеченных слов составило только 92,09%, поэтому документы не считаются безопасно замаскированными и блокируются целиком. p50 2,281 с, p95 2,434 с, максимум 2,601 с.
- После добавления блокировки чистый фасад 4000×3000 повторно дал `document.suspected=false`, 0 лиц, 0 текстовых областей и 0 номеров.
- Воспроизводимые evaluators: `server/python/evaluate_anonymization.py` и `server/python/evaluate_document_text.py`; версии и SHA-256 Python-зависимостей и моделей закреплены в `server/requirements-anonymization.txt` и `server/python/anonymizer-models.sha256`.
- `LocalImageAnonymizer` содержит внутреннюю очередь; production-конфигурация `PHOTO_ANONYMIZATION_CONCURRENCY=1` не допускает одновременный запуск двух тяжёлых Python-процессов в одном API-процессе. Отдельный тест подтверждает порядок `start/end/start/end` для двух параллельных запросов.
- Компактный машиночитаемый протокол документной проверки сохранён в `tasks/legal-photo-anonymization-document-validation.json`.
- Хеши 4/4 локальных ONNX-моделей совпали с закреплёнными SHA-256.
- Ресурсная проверка на 12 и 24 Мп выполнена с параллелизмом 1: 6/6 запусков успешны, p50 4,871 с, p95 7,308 с, максимум 7,512 с, максимальный peak working set 657,7 МБ. На обоих выходах EXIF отсутствует. Контроль с отсутствующей обязательной моделью завершился кодом 2 и нулевым выходным файлом, то есть изображение не было выпущено из fail-closed этапа.
- Машиночитаемый протокол ресурсной проверки сохранён в `tasks/legal-photo-anonymization-resource-benchmark.json`. Замер выполнен локально на Intel Core i3-7020U, 2 физических ядрах и 7,9 ГБ RAM; он обосновывает стартовый лимит `concurrency=1` и не заменяет проверку на Timeweb.
- Реальный сквозной вызов через Node-класс `LocalImageAnonymizer` принял чистый фасад 12 Мп за 4,524 с и вернул только очищенный JPEG. Тем же Node-контуром реальный документ FUNSD был заблокирован кодом `PHOTO_ANONYMIZATION_DOCUMENT_SUSPECTED` до возврата рабочей копии вызывающему сервису.
- Изолированный benchmark на Timeweb выполнен без переключения релиза и перезапуска сервисов: 6/6 запусков на 12–24 Мп, p50 2,185 с, p95 2,230 с, максимум 2,240 с, максимальный RSS 690,6 МиБ. Контроль отсутствующей модели вернул код 2 и нулевой файл. После проверки API и worker остались `active`, readiness подтвердил PostgreSQL, Redis и хранилище как `up`.
- Для изолированного Python-окружения на Timeweb установлен системный пакет `python3.12-venv`; Ubuntu одновременно обновила Python 3.12.3 с ревизии `ubuntu0.15` до `ubuntu0.16`. Активный релиз и его конфигурация не изменялись.

Нативный автопоиск `npm test` на Windows после успешно завершённого файла сравнения не завершает процесс. Принудительный `--test-force-exit` выполняет все 194 функциональных сценария, но затем вызывает восемь файловых libuv-assertion на закрытии уже прошедших HTTP-тестов. Эти файлы проходят отдельно и в явных пакетах; это ограничение завершения Windows Node test runner, которое следует исправлять отдельной инфраструктурной задачей, а не функциональная ошибка приложения.

Полный прежний E2E-набор отдельно выявил два дефекта вне этого этапа: устаревшее обращение к карточке материала и контраст медной подписи на странице сравнения 3,04:1. Они не скрыты и не исправлялись в юридической ветке, чтобы не смешивать её с редизайном.

## Аудит даты начала обработки

- Служебный адрес `stage-payment-…@vizhufasad.invalid` относится к автоматическому тесту.
- `sto.avtonomia@gmail.com`, по уточнённому подтверждению Максима, является его вторым адресом email и использовался владельцем для тестирования сайта: одна сессия, проект, исходное фото и завершённая проверка фото; генераций и платежей нет.
- По доступным данным внешних субъектов до заявленной даты 03.09.2026 не обнаружено. Уведомление РКН автоматически не изменялось и ничего в РКН не отправлялось.

## Остаточные риски и условия выпуска

1. Не обещается 100% обнаружение мелких, закрытых, размытых, рукописных и слабоконтрастных данных.
2. Реальная регрессия российских номеров и fail-closed блокировка 50 реальных документов пройдены. Для российских адресных табличек остаётся провести широкую реальную размеченную проверку; 100% распознавание не заявляется.
3. Модели ONNX намеренно не добавлены в Git. Перед выпуском их нужно установить из зафиксированных источников, сверить `server/python/anonymizer-models.sha256`, создать отдельный Python venv и включить функцию переменными окружения.
4. Timeweb benchmark 12–24 Мп при параллелизме 1 пройден. Для первоначального выпуска сохраняются лимиты: один процесс обезличивания одновременно, минимум 2 ГБ RAM на процесс и таймаут 30 секунд; повышать параллелизм без отдельного нагрузочного теста нельзя.
5. До заявленной даты начала обработки 03.09.2026 публичный сбор новых персональных данных расширять нельзя; отдельно ожидается ответ РКН по уже поданному уведомлению.
6. Нужны отдельное подтверждение Максима на публикацию и отдельное окно выпуска. До него production остаётся без этих изменений.
7. Временные локальные каталоги `D:\VIZHUFASAD\rf-acceptance-temp`, `D:\VIZHUFASAD\document-acceptance-temp` и `D:\VIZHUFASAD\anonymization-benchmark-temp` не входят в Git и production, но их удаление было заблокировано политикой безопасности инструмента. Их необходимо удалить отдельной явно разрешённой операцией после сохранения компактных отчётов.
8. На Timeweb сохранён закрытый root-only кандидат `/opt/vizhufasad-anonymizer-check-20260831` объёмом 385 МБ для повторяемости benchmark и подготовки выпуска. Он не подключён к API/worker и должен быть либо перенесён в утверждённые shared-пути при выпуске, либо удалён после отказа от выпуска.

# Выпускной чек-лист юридического обезличивания

Дата подготовки: 31.08.2026. Этот документ не является разрешением на публикацию.

## До окна выпуска

- [ ] Получен ответ РКН либо отдельно подтверждено, что публикация допустима с 03.09.2026.
- [ ] Максим отдельно разрешил commit/push и production-деплой этой ветки.
- [ ] Зафиксировано решение по остаточному риску слабоконтрастных российских адресных табличек; 100% распознавание нигде не обещается.
- [ ] Активный release, состояние API/worker, свободная память и резервная точка зафиксированы read-only проверкой.
- [ ] Миграция `0022_preauth_personal_data_consent.sql` просмотрена и подготовлена к применению без удаления существующих данных.

## Runtime обезличивания

- [ ] Отдельный Python venv создан в `/opt/vizhufasad-shared/anonymizer-venv`.
- [ ] Зависимости установлены только из `server/requirements-anonymization.txt` с `--require-hashes`.
- [ ] Четыре ONNX-модели размещены в `/opt/vizhufasad-shared/anonymizer-models` и сверены с `server/python/anonymizer-models.sha256`.
- [ ] Runtime и модели недоступны для записи пользователю API/worker.
- [ ] Установлены `PHOTO_ANONYMIZATION_*` пути, `PHOTO_ANONYMIZATION_TIMEOUT_MS=30000`; до canary функция остаётся `PHOTO_ANONYMIZATION_ENABLED=false`.
- [ ] Параллелизм обезличивания ограничен одним процессом; одновременно не выполняются два тяжёлых задания.

## Локальные ворота кандидата

- [ ] `npm.cmd run check` — успешно.
- [ ] `npm.cmd --prefix server run check` — успешно.
- [ ] Unit/integration/E2E из отчёта пройдены на точном кандидате.
- [ ] `git diff --check` — без ошибок.
- [ ] Проверены desktop 1440 px и mobile 360/390 px: согласие до email, две пустые отметки перед фото, корректная ошибка документа.

## Production canary без платного GenAPI

- [ ] Применить additive-миграцию, развернуть новый неизменяемый release и не менять старый каталог.
- [ ] Включить обезличивание только после готовности venv и 4/4 моделей.
- [ ] Чистое тестовое фото владельца проходит локальное обезличивание, EXIF отсутствует, рабочая копия отличается от приватного пути исходника.
- [ ] Реальный тестовый документ блокируется до photo assessment, очереди генерации и резервирования ВФ-коина.
- [ ] Временно недоступная обязательная модель даёт fail closed: GenAPI-запросов 0, списаний 0, пользователю показана безопасная ошибка.
- [ ] API, worker, PostgreSQL, Redis и хранилище остаются `active/up`; RAM процесса укладывается в предел до 900 МиБ.

## Платный canary

- [ ] Выполняется только после отдельного непосредственного подтверждения Максима количества запусков и рублёвого лимита.
- [ ] Максимум один запуск на заранее согласованном тестовом фото; до запуска фиксируются баланс и идентификатор тестового проекта.
- [ ] Подтверждено, что в GenAPI ушёл ключ только очищенной рабочей копии, а не приватного исходника.
- [ ] Проверены результат, статус, один расход ВФ-коина и отсутствие повторного списания.

## Откат

- [ ] При любой ошибке сначала установить `PHOTO_ANONYMIZATION_ENABLED=false` и перезапустить только API/worker.
- [ ] Если ошибка связана с release, вернуть атомарную ссылку на предыдущий release по действующему Stage 13 runbook.
- [ ] Не откатывать additive-миграцию удалением таблицы: сохранённые доказательства согласия не уничтожать.
- [ ] Зафиксировать код ошибки, количество затронутых тестовых операций и отсутствие передачи неочищенного файла.


# Этап 13: отчёт о готовности редизайна

Дата последней проверки: 23 августа 2026 года

Ветка: `codex/stage-13-redesign`

Состояние: развёрнуто на изолированном staging; production не переключён

Staging: `https://46-149-67-190.sslip.io/`

Развёрнутый commit: `67126cb`

## Что изменено

### Публичный путь

- утверждённый первый экран с автоматической сменой исходника и вариантов одного дома;
- ускоренная карусель с responsive WebP-копиями для мобильных экранов;
- понятный стартовый CTA и публичная техническая проверка фотографии;
- самостоятельный путь без заявки, телефона, дизайнера, оператора и ручной проверки;
- блок «Как это работает», объяснение сохранения геометрии и автоматического QC;
- восстановленная редакционная композиция «От фотографии до решения за 3 шага» с одной утверждённой парой дома «до/после» вместо разных зданий;
- три одобренных демонстрационных направления на одном доме;
- галерея с исходником и результатом, фильтрами, полноэкранным просмотром, Escape и mobile swipe;
- бесплатный старт отделён от трёх платных пакетов без изменения утверждённых цен;
- расширенный FAQ о хранении фото, ограничениях AI, списании и автоматическом возврате кредита;
- отдельные SEO-страницы фасада по фото, стилей/материалов и партнёрского предложения.

### Продуктовый путь

- обязательное версионируемое согласие до передачи фото в приватное хранилище и AI-обработку;
- пошаговый `/app/new` с качеством Standard/Pro, стилями, материалами, палитрой, ограничениями и подтверждением стоимости;
- сохранение черновика при переходах и reload;
- готовый результат с до/после, избранным, историей, версиями, редактором, 4K, сравнением и балансом;
- явное подтверждение прохождения автоматического контроля качества;
- конфигурация пользователя автоматически входит в задание генератору.

### Аналитика и экономика

- обезличенные события воронки без фото, контактов, cookie и платёжных идентификаторов;
- агрегированные внутренние метрики выручки, возвратов, provider cost и незакрытых расходов;
- цена provider учитывается в рублёвой метрике только при `cost_currency=RUB`;
- платный трафик закрыт воротами до подтверждения полной себестоимости успешного результата.

### SEO и rich results

- canonical, Open Graph, Twitter metadata, sitemap и robots;
- `Organization`, `WebSite`, `SoftwareApplication`, `FAQPage`, `BreadcrumbList`, `ItemList` и `ImageObject` только для видимого контента;
- вымышленные `Review`, `AggregateRating`, клиенты и сроки не добавлены;
- локальная проверка JSON-LD и загрузки изображений пройдена;
- официальный Google Rich Results Test 23 августа 2026 года обнаружил два валидных элемента: `Organization` и `SoftwareApplication`;
- у `SoftwareApplication` есть только необязательное предупреждение об отсутствии `aggregateRating`. Поле намеренно не добавлено: подтверждённого рейтинга клиентов пока нет.

### Производительность опубликованного staging

Мобильный PageSpeed Insights измерен на одном и том же опубликованном URL. Это лабораторные результаты, поэтому общий балл и Speed Index могут колебаться между прогонами.

| Метрика | До оптимизации | После оптимизации |
| --- | ---: | ---: |
| Performance | 67 | 65 |
| Accessibility | 100 | 100 |
| Best Practices | 100 | 100 |
| SEO | 100 | 100 |
| FCP | 3,3 с | 3,3 с |
| LCP | 7,4 с | 6,9 с |
| TBT | 10 мс | 30 мс |
| CLS | 0 | 0 |
| Speed Index | 4,6 с | 6,0 с |

После измерения внешние Google Fonts заменены на self-hosted шрифты, а для версионированных Next.js-ресурсов, изображений и шрифтов на staging настроено длительное кэширование. LCP улучшился на 0,5 секунды; рекомендация по сроку кэширования сократилась с 1 214 КиБ до 1 КиБ. Следующий измеримый резерв — подготовка более лёгких вариантов hero-изображений, для которых PageSpeed оценивает экономию примерно в 383 КиБ.

## Выполненные проверки

| Проверка | Результат |
| --- | --- |
| `npm.cmd run typecheck` | успешно |
| `NEXT_OUTPUT=export npm.cmd run build` | успешно, 14 статических страниц |
| Stage 10 frontend E2E, полный прогон | 8 из 8 |
| Stage 12 generation tools E2E, полный прогон | 4 из 4 |
| Финальный E2E готового Standard-результата | 1 из 1 |
| Публичные страницы: изображения, JSON-LD, Axe critical/serious, overflow | успешно |
| Главная: 360, 390, 768, 1366 и 1440 px | успешно |
| Галерея 360 px: фильтр, lightbox, Escape, accessibility | успешно |
| Staging: PostgreSQL, Redis, приватный S3 и API readiness | успешно |
| Staging: главная, галерея, стили, SEO-страницы и публичный каталог | успешно |
| Staging: первый экран и изображения в реальном браузере | успешно, ошибок консоли нет |
| PageSpeed mobile до/после оптимизации | LCP 7,4 → 6,9 с; Accessibility/Best Practices/SEO 100 |
| Google Rich Results Test | 2 валидных элемента без ошибок; 1 необязательное предупреждение без фиктивного рейтинга |
| Production до/после развёртывания staging | неизменный SHA-256 `0fc9d1dd20cd6e58cac51c666a96b8603b0dce76f6982b74c64e7f35058ed716` |

Временные PNG визуальной проверки находятся в локальном игнорируемом каталоге `tmp/` и не предназначены для публикации.

## Что намеренно не проверялось повторно

- полный серверный набор, платежи и реальная GenAPI-генерация не запускались после последнего UI-инкремента, потому что их бизнес-логика не изменялась;
- Robokassa не менялась и не возвращалась в активную работу этапа 11;
- платные provider-запросы не выполнялись ради визуальной проверки.

## Обязательные live-пункты перед завершением

Эти пункты нельзя подтвердить локальной статической сборкой:

1. Пройти один сквозной сценарий с production SMTP, очередью, worker, GenAPI и QC после отдельного разрешения на платный provider-тест.
2. Проверить доступность checkout и ранее подтверждённого платёжного контура без повторной ненужной оплаты.
3. Убедиться, что production-логи не содержат секретов, cookie, контактов и фотографий.
4. Получить отдельное разрешение владельца на атомарное переключение `vizhufasad.ru`.

## Известные ограничения

- Публично показаны только три стиля с одобренными изображениями; остальные доступны в мастере без случайных демонстраций.
- Подтверждённых отзывов клиентов пока нет, поэтому блок отзывов и review-разметка отсутствуют.
- Производственный снимок уже показывает 40 ₽ provider cost на единственный завершённый результат и 240 ₽ затрат на задачи `failed_refunded`; выборка слишком мала для публикации нового прайса. До изменения тарифов или запуска рекламы нужно накопить не менее 30 успешных платных генераций и добавить остальные переменные расходы.
- Визуализация остаётся концепцией, а не строительным проектом, сметой или расчётом материалов.
- Реальный Google rich result не гарантируется даже при валидной разметке.
- На staging отключены платежи, Standard/Pro-генерация и worker, чтобы визуальная проверка не создавала реальные списания; production-процессы на порту `8081` не перезапускались.

## Откат

Production-шаблоны используют неизменяемые release-каталоги и атомарную ссылку `/opt/vizhufasad-current`. Быстрый откат возвращает ссылку на предыдущий релиз и перезапускает API/worker. База данных автоматически не откатывается, чтобы не потерять новые оплаты, проекты и транзакции.

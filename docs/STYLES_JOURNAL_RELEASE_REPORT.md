# Выпуск журнала фасадных стилей

Дата публикации: 26 августа 2026 года, 13:20 UTC  
Production commit: `bed75c3`  
Активный release: `/opt/vizhufasad-releases/prod-styles-bed75c3`  
Предыдущий release для отката: `/opt/vizhufasad-releases/prod-topups-0779dbe`

## Опубликовано

- единая журнальная страница `/styles` с десятью стилями;
- по три отдельных изображения разных домов для каждого стиля;
- статьи, материалы, палитры и рекомендации по применению;
- desktop-сетка из трёх карточек и мобильная scroll-snap карусель;
- фирменное оформление карточек: медно-графитовая роза ветров, длинный нижний луч и знак `ВФ`;
- постоянные редиректы старых страниц `/styles/<slug>` на соответствующие разделы единой страницы.

## Проверки

- локальные `npm run typecheck` и production export — успешно;
- серверная production-сборка: 21 статическая страница — успешно;
- `/styles` возвращает HTTP 200 и новый заголовок страницы;
- 30 из 30 изображений в `/facade-styles/` доступны по HTTPS;
- в браузере обнаружены 30 карточек, 30 роз ветров и 30 знаков `ВФ`;
- видимые изображения загружены с натуральной шириной 720 px;
- ошибок и предупреждений браузерной консоли нет;
- API и worker имеют статус `active`; database, Redis и storage — `up`;
- nginx-конфигурация прошла проверку;
- после рестарта в логах API и worker не найдено необработанных ошибок.

## Резервная точка

Перед переключением создан каталог
`/var/backups/vizhufasad/prod-styles-bed75c3-before-switch-20260826`.
В нём сохранены путь предыдущего release, состояния сервисов и PostgreSQL dump размером 171311 байт с режимом `600`.

Первая попытка серверной установки остановилась до переключения из-за недоступного npm-кэша пользователя `www-data`. Production оставался на предыдущем release. Сборка была повторена с отдельным кэшем, после чего переключение и smoke прошли успешно.
